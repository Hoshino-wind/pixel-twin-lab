interface:
  display_name: "Pixel Twin UI Editor"
  short_description: "Apply references directly to existing frontend UIs"
  default_prompt: "Use $pixel-twin-lab to apply this visual reference directly to the existing UI without changing business behavior."
